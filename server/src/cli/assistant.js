const body={screen:process.argv[2]||'',field:process.argv[3]||'',issue:process.argv[4]||''}
console.log(JSON.stringify({lines:(()=>{const a=[];const s=(body.screen||'').toLowerCase();const f=(body.field||'').toLowerCase();const i=(body.issue||'').toLowerCase();if(s==='aadhaar'){a.push('Select the ZIP file and enter the 4-digit share code');a.push('Make sure the ZIP password matches the share code');a.push('Verify then review before submit')}if(s==='document'){a.push('Capture or paste text');a.push('Fix any misread fields before submit')}if(s==='face'){a.push('Keep your face centered');a.push('Blink once and turn slightly')}if(f==='share_code'){a.push('Use the 4 digits set while downloading the ZIP')}if(i.includes('network')){a.push('Data is queued offline and will sync automatically')}if(a.length===0){a.push('Follow the on-screen steps and continue')}return a})()} )
